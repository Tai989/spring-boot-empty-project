apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: spring-boot
  name: spring-boot-gateway-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-boot-gateway
  template:
    metadata:
      labels:
        app: spring-boot-gateway
    spec:
      containers:
        - name: spring-boot-gateway
          image: 192.168.0.111:8050/spring-boot-gateway
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8877
---
apiVersion: v1
kind: Service
metadata:
  name: spring-boot-gateway-service
spec:
  selector:
    app: spring-boot-gateway
  ports:
    - name: http
      port: 8877
      targetPort: 8877
  type: NodePort